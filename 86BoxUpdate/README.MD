# Scuffed 86Box Updater
This is an updater script for 86Box that pulls the latest Jenkins build and clones it's required assets. It's fairly scuffed and will automatically overwrite the executable even if there is no update available. Hell even the Git related things are raw console commands lol.

You need Git installed for this to work properly.