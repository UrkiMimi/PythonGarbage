# YoutubeRandomizer
A hacked together script that lets you get random videos from somebody's channel! Channel IDs are only supported.

### Usage
```
python randomizer.py [channel ID] --regenerate [Regenerates playlist cache (debug)]`
```
